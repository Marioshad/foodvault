[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = [
  "esbuild backend/src/index.ts --platform=node --packages=external --bundle --format=cjs --outfile=dist/index.js"
]

[start]
cmd = "NODE_ENV=production node dist/index.js"